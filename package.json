{
  "name": "contentful-ui-colorpicker",
  "version": "1.0.0",
  "description": "UI Extension to add Color Picker to Simple Text field's appearance.",
  "scripts": {
    "update": "gulp build && contentful-extension update --space-id $SPACE_ID --force",
    "dev": "contentful-extension update --space-id $SPACE_ID --force --src https://localhost:3000 && python https"
  },
  "author": "Tomas Roggero <hola@tomasroggero.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-inline-source": "^3.0.0",
    "gulp-minify-inline": "^0.2.1",
    "gulp-rename": "^1.2.2"
  }
}
